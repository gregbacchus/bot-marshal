---
title: bot-marshal
description: An egress firewall for AI agents — default-deny per-request policy, credentials injected at the boundary, and a complete audit trail.
template: splash
editUrl: false
hero:
  tagline: An egress firewall for AI agents, coding bots, and other untrusted automation. Default-deny per request, credentials injected at the boundary, and everything audited.
  actions:
    - text: Get started
      link: /bot-marshal/getting-started/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/gregbacchus/bot-marshal
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

<div class="bm-eyebrow">Single binary · Rust · default-deny</div>

<div class="bm-flow">
  <span>capture</span><i>→</i>
  <span>identity</span><i>→</i>
  <span>policy chain</span><i>→</i>
  <span>transforms</span><i>→</i>
  <span>upstream</span><i>·</i>
  <span class="deny">403 + reason</span>
</div>

## Why

An agent on a developer machine or in CI has unrestricted outbound access. It can exfiltrate
secrets, fetch and execute arbitrary content, or be steered by a prompt injection into
contacting attacker infrastructure. Firewall rules are too coarse to help: agents legitimately
need GitHub, npm, PyPI and LLM APIs, and those same hosts are exfiltration channels. The
boundary has to understand HTTP, not just IPs.

<CardGrid>
  <Card title="Default-deny, per request" icon="approve-check">
    Policy is a short-circuiting chain of layers — `denylist`, `allowlist`, `rules`, `dlp`,
    `mcp`, `judge`. The `allow` default has to be opted into, in config, in writing.

    [Policy layers →](/bot-marshal/configuration/policy-layers/)
  </Card>
  <Card title="The agent never holds the key" icon="seti:lock">
    Credentials are injected at the boundary and are unconditional per destination. They never
    reach a log, an audit record, or the judge.

    [Secret injection →](/bot-marshal/configuration/transforms/)
  </Card>
  <Card title="Streaming stays streaming" icon="rocket">
    Bodies stream by default. A transform that needs the body buffered declares it, with a cap
    — so SSE, WebSockets and large uploads keep working.

    [Concepts →](/bot-marshal/concepts/)
  </Card>
  <Card title="A trail you can answer questions with" icon="open-book">
    Every request produces an append-only evidence trail: which layer decided, on what facts,
    and what the agent was told.

    [Observability →](/bot-marshal/observability/)
  </Card>
</CardGrid>

## Try it

```bash
cargo build --release
alias marshal=./target/release/marshal

marshal config check
marshal ca init
marshal serve --listen 127.0.0.1:8080
```

Point an agent at `http://127.0.0.1:8080` and watch it get told exactly why it was refused.

[Full walkthrough →](/bot-marshal/getting-started/) · [Documentation index →](/bot-marshal/overview/)
